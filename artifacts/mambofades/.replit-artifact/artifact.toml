kind = "web"
previewPath = "/"
title = "MamboFades"
version = "1.0.0"
id = "artifacts/mambofades"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25656

[services.development]
run = "pnpm --filter @workspace/mambofades run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mambofades", "run", "build" ]
publicDir = "artifacts/mambofades/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25656"
BASE_PATH = "/"
